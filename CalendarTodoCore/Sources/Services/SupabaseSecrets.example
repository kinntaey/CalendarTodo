import Foundation

/// Copy this file to SupabaseSecrets.swift and fill in your Supabase credentials.
/// SupabaseSecrets.swift is excluded from git via .gitignore
enum SupabaseSecrets {
    static let url = "YOUR_SUPABASE_URL"
    static let anonKey = "YOUR_SUPABASE_ANON_KEY"
}
